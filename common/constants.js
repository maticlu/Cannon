export const vw = Math.max(
    document.documentElement.clientWidth,
    window.innerWidth || 0
);

export const vh = Math.max(
    document.documentElement.clientHeight,
    window.innerHeight || 0
) + 15;

export const EXPLOSION_TIME = 1000;

export const COLOR_FIRE = {
    r: 225,
    g: 142,
    b: 0
}